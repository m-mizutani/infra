name: push-warren-image

on:
  push:
    branches: [ main ]
    paths:
      - 'images/warren/**'
      - '.github/workflows/push-warren-image.yml'
      - '.github/workflows/deploy-cloud-run.yml'

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-cloud-run.yml
    with:
      service_name: warren
      image_path: images/warren
      project_id: mztn-service
    secrets: inherit
