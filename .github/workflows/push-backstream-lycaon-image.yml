name: push-backstream-lycaon-image

on:
  push:
    branches: [ main ]
    paths:
      - 'images/backstream-lycaon/**'
      - '.github/workflows/push-backstream-lycaon-image.yml'
      - '.github/workflows/deploy-cloud-run.yml'

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-cloud-run.yml
    with:
      service_name: backstream-lycaon
      image_path: images/backstream-lycaon
      project_id: mztn-service
    secrets: inherit