name: push-backstream-tamamo-image

on:
  push:
    branches: [ main ]
    paths:
      - 'images/backstream-tamamo/**'
      - '.github/workflows/push-backstream-tamamo-image.yml'
      - '.github/workflows/deploy-cloud-run.yml'

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-cloud-run.yml
    with:
      service_name: backstream-tamamo
      image_path: images/backstream-tamamo
      project_id: mztn-service
    secrets: inherit